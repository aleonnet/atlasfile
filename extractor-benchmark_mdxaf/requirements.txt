# Extrator sob teste
markitdown[all]

# Libs de extracao do AtlasFile — versoes pinadas iguais ao backend/requirements.txt
# para o extrator rodar identico a producao.
pymupdf>=1.25.0
pdf2image==1.17.0
pytesseract==0.3.10
Pillow>=10.0.0
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
extract-msg==0.55.0

# Settings do extrator (app.config) — habilita OCR corretamente
pydantic-settings
