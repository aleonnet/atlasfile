# Override para OpenSearch Dashboards (montado no container).
# Conexão com OpenSearch; senha pode ser sobrescrita por OPENSEARCH_PASSWORD no compose.
server.port: 5601
server.host: "0.0.0.0"
opensearch.hosts: ["https://opensearch:9200"]
opensearch.username: "admin"
opensearch.password: "Kaid0Search!2026X"
opensearch.ssl.verificationMode: none
opensearch.requestHeadersAllowlist: ["securitytenant", "authorization", "Authorization"]
